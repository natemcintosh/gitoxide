name: Test GitHub Action
on: [pull_request]

jobs:
  run:
    name: Spell Check with Typos
    runs-on: ubuntu-latest
    steps:
    - name: Checkout
      uses: actions/checkout@v4

    - name: Check spelling of rpo
      uses: crate-ci/typos@master
      with:
        config: ../../_typos.toml
